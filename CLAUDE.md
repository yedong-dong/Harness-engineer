# CLAUDE.md

## 我是谁

互联网测试工程师，面向 App 测试（客户端 + 服务端）。

这是一个个人工作台，核心目标是**用 AI 提高测试效率**，同时探索 AI/Skill 本身的工作方式。

## 两种工作模式

| 模式 | 触发场景 | 我对你的期望 |
|------|---------|-------------|
| **测试工作** | 给 PRD、设计文档、接口文档、Bug 日志 | 生成用例、分析风险、整理报告——直接出活 |
| **AI 学习** | 探索 Skill 机制、MCP 架构、Agent 协作、提效工具 | 解释概念和设计思路，让我理解 AI 如何提效，而不只是给结果 |
| **链接转教程** | 给一个网页/文章链接，说「总结并教我」 | 用五段式（原理→Demo→已有知识连接→练习题→答案）产出学习资料，写入 `docs/ai-learning/` |
| **版本全量分析** | 说「版本全量分析」「完整分析XX版本」「XX版本全套」 | 串行执行：业务分析 → 测试用例 → API 集合，中间两次暂停确认 |

## 思考模式

当我的问题模糊、犹豫、或只是想探索可能性时，不要直接给一个答案。用以下方式帮我理清自己的想法。

### 触发条件

- 表述模糊，没有明确要求（「你觉得呢」「我应该怎么做」「有哪些方向」）
- 在多个选项间犹豫（「A 还是 B」「不知道选哪个」）
- 探索性提问（「我能做什么」「有哪些可能性」）
- 需要决策但信息不完整

### 你的行为规则

1. **先收敛，再深入**：用 2-4 个结构化选项帮我聚焦，不无限发散
2. **选项必须有对比**：每个选项标注适合什么场景、需要什么投入、能得到什么结果
3. **推荐放在第一位**，标注（推荐），并说明推荐理由
4. **始终保留「其他」出口**：我可以跳出框架自己描述
5. **追问不超过两轮**：两轮后基于已有信息给出判断，不等我无限澄清

### 选项模板

| 选项 | 适合场景 | 投入 | 预期收益 |
|------|---------|------|---------|
| A（推荐） | ... | ... | ... |
| B | ... | ... | ... |
| C | ... | ... | ... |

推荐理由：{一句话}

### 不触发的情况

- 已有明确指令 → 直接执行，不要反问
- 简单的技术问答 → 直接答，不要给选项
- 纯执行类任务（写代码、跑测试、改文件）

## 语言

无论我用中文还是英文提问，所有回复统一使用中文。

## 测试风格

- 优先级：P0（核心阻断）→ P1（重要功能）→ P2（边缘场景）
- 每条用例：标题、前置条件、步骤、预期结果
- 覆盖路径：正常 → 边界值 → 异常 → 状态流转 → 数据一致性
- 输出优先用表格、清单、树状结构

## XMind 标签

用例只标平台标签，按端分发：

| 标签 | 场景 |
|------|------|
| `客户端` | UI 交互、页面展示、跳转、弹窗 |
| `服务端` | 接口逻辑、数据一致性、并发/幂等、定时任务 |
| `Web` | 管理后台、CMS 配置、运营工具 |

## 知识库

目录结构：

```
docs/
├── README.md                    ← 总索引
├── AIdoc/                       ← 岗位 Agent 应用指南
│   ├── agent-role-application.md
│   ├── roles/                   ← 各岗位详细文档
│   └── reference/               ← 外部参考链接
├── ai-learning/                 ← AI 技术学习资料
├── workflow-reference/          ← Agent 架构模式文档
├── business-analysis/           ← PRD 业务深层分析
├── reference/                   ← 外部工具/平台分析参考
└── temp/                        ← 未归类的临时文档
```

- AI 学习资料：[docs/ai-learning/](docs/ai-learning/) — Tool Calling、MCP Server、Agent Workflow、Prompt/Skill、RAG、Eval 的详解 + 练习题
- 岗位 Agent 应用：[docs/AIdoc/agent-role-application.md](docs/AIdoc/agent-role-application.md) — 六岗位（产品/开发/测试/数据分析/PM/运营）的 Agent 应用指南

### 文档自动管理

当我生成或删除 `docs/` 下的文件时，自动更新 `docs/README.md` 总索引。README.md 结构：
- 按目录分组
- 每个文件一行链接，附简短说明
- 无文件（空目录）列表
- 标注哪些是临时文件（temp/ 下的）

## 业务敏感性分析

拿到 PRD / 需求文档时，不只提取功能点，要挖出产品和开发容易遗漏的逻辑漏洞。方法分四步：

```
PRD 文字（What）
     │
     ▼
① 业务理解（Why + Who）
  · 这个需求为什么现在做？不做的后果？
  · 涉及哪些实体？关系是什么？影响什么业务指标？
     │
     ▼
② 技术反推（How）
  · 后端大概率怎么实现？用什么数据结构/算法/状态机？
  · 推断必须有依据（PRD 暗示 / 技术文档 / 行业通用做法）
  · 不确定时标记「待确认」
  · 关键逻辑用伪代码表达——伪代码比纯文字更能暴露问题
     │
     ▼
③ 漏洞挖掘（What Could Go Wrong）
  · 正常路径的隐藏风险：原子性、数据一致性、缓存延迟
  · 极端用户路径：不按产品设想的"正常路径"走
  · PRD 模糊词追问："连续""下次""实时""每日""仅一次"——具体条件是什么？
  · 每个漏洞附带：场景推演 + 为什么易被遗漏 + 业务后果
     │
     ▼
④ 测试要点（How to Verify）
  · 针对漏洞给出验证方向（不是罗列用例）
  · 关键验证用伪代码
  · 附"待确认清单"——跟产品/开发沟通用的讨论清单
```

### 漏洞挖掘的四个切入点

| 切入点 | 问自己 | 易漏人群 |
|--------|--------|---------|
| 分布式场景 | "如果发奖成功但写状态失败，用户刷新后能看到什么？" | 产品+开发 |
| 极端路径 | "用户第1天签到→第2-6天不登录→第7天登录，会发生什么？" | 产品 |
| 定义模糊 | "PRD 说'下次登录再弹'——杀进程重开算不算登录？" | 产品+开发 |
| 利益冲突 | "这个功能对A有利，对B是否有损？用户是否有动机绕过？" | 产品 |

### 输出位置

分析报告写入 `PRD/<项目>/output/<需求名>.md`（与 .xmind 同目录），若无 PRD 目录则写入 `docs/notes/business-analysis/`。

完整方法论见 Skill：`prd-business-analysis`

## 版本全量分析工作流

三个 Skill 可独立触发，也可通过「版本全量分析」组合触发。组合时按以下顺序串行执行：

```
                          用户输入：「版本全量分析 Achat 3.22」
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│  Skill 1: prd-business-analysis（业务+技术深层分析）          │
│                                                              │
│  输入：需求文档 + 技术方案                                     │
│  流程：业务理解 → 技术推断 → ★暂停确认 → 漏洞挖掘 → 输出报告   │
│  输出：PRD/<App>/<版本>/output/<需求>-深层分析.md              │
│  耗时：最长（深度思考，不可并行）                               │
└──────────────────────┬───────────────────────────────────────┘
                       │ 产出：漏洞清单 + 业务风险点
                       ▼
┌──────────────────────────────────────────────────────────────┐
│  Skill 2: prd-testcase-xmind（测试用例生成）                   │
│                                                              │
│  输入：需求文档 + 技术方案 + (可选)Skill 1 漏洞清单            │
│  流程：四维提取 → ★暂停澄清 → SubAgent 并行生成 → 评估 → 导出  │
│  输出：PRD/<App>/<版本>/output/<需求>.xmind                   │
│  耗时：中等（SubAgent 并行加速）                                │
│  ★ 可并行：与 Skill 1 同时启动（如果不等分析结果）               │
└──────────────────────┬───────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────┐
│  Skill 3: techdoc-to-openapi（API 集合生成）                   │
│                                                              │
│  输入：技术方案（proto 定义）                                   │
│  流程：解析 proto → ★暂停确认 → 生成 Bruno 集合                │
│  输出：PRD/<App>/<版本>/output/api/ (Bruno 可直接打开)         │
│  耗时：较短（纯转换，无深度推理）                               │
│  ★ 可并行：与 Skill 1/2 同时启动（互不依赖）                    │
└──────────────────────────────────────────────────────────────┘
                       │
                       ▼
                全部产出在统一目录下：
                PRD/<App>/<版本>/output/
                    ├── <需求>-深层分析.md
                    ├── <需求>.xmind
                    └── api/
                        └── (Bruno 集合)
```

### 独立使用 vs 组合使用

| 触发词 | 行为 |
|--------|------|
| 「帮我分析这个 PRD」 | 仅 prd-business-analysis |
| 「生成测试用例」 | 仅 prd-testcase-xmind |
| 「生成 API / 导出接口」 | 仅 techdoc-to-openapi |
| 「版本全量分析」「完整分析 XX 版本」「XX 版本全套」 | 串行执行三个 Skill，每阶段暂停确认 |

### 并行优化

- Skill 2（testcase-xmind）和 Skill 3（techdoc-to-openapi）互不依赖，可与 Skill 1 同时启动
- 如果用户赶时间：「先并行跑三个 Skill，分析结果出来后再交叉校验」

## 执行偏好

- 只读操作（测试命令、curl、SELECT、读日志/配置）→ 直接执行
- 写操作（INSERT/UPDATE/DELETE）→ 先展示命令，等我确认
- 画图用纯文本 ASCII，不用 Mermaid

# CLAUDE.md

## 角色

我是一名互联网测试工程师。给你的任何文档（PRD、设计文档、接口文档），默认意图是生成测试用例或分析测试风险。

## 语言

无论我用中文还是英文提问，所有回复统一使用中文。

## 测试用例规范

- 优先级：P0（核心阻断）→ P1（重要功能）→ P2（边缘场景）
- 每条用例包含：标题、前置条件、步骤、预期结果
- 用例覆盖：正常路径 → 边界值 → 异常场景 → 状态流转 → 数据一致性
- 回答优先使用表格、清单、树状结构

## XMind 标签（仅三种，导入后按端分发）

每条用例的 XMind 标签只输出平台标签，不出需求标签和自定义标签。

| 标签 | 适用场景 |
|------|---------|
| `客户端` | 用户可见的 UI 交互、页面展示、跳转、弹窗 |
| `服务端` | 接口逻辑、数据一致性、并发/幂等、定时任务 |
| `Web` | 管理后台、CMS 配置、运营工具 |

## PRD → 测试脑图（Skill: prd-testcase-xmind）

当你说 PRD、测试用例、XMind、需求转测试、测试脑图 时自动触发。

### 流程

1. 给 PRD 文字 → Agent 分析，提取 Feature/Rule/State/Risk 四维要素
2. Agent 暂停，展示分析结果 + 澄清问题 → 等你回复
3. 你自然回复（有补充就说，没问题就确认）
4. Subagent 按模块并行生成用例 → 主 Agent 合并 → 覆盖自检 → 导出 .xmind

### Review 无命令词

澄清阶段直接自然回复即可。Agent 默认按 pass_with_notes 处理，只有明确表达"不对/重来"才触发修正。

### 覆盖自检四维

生成前内部核对：□ Feature □ Rule □ State □ Risk，遗漏自动补 case。

## 其他常用场景

- **接口文档 → 接口用例**：贴接口文档自动生成
- **Bug 分析**：贴报错日志/现象即可
- **测试报告**：给原始测试结果，自动整理
- **造测试数据**：描述场景，生成构造脚本

## 自动化偏好

- 测试命令（go test / npm test / pytest 等）直接执行，不询问
- curl 发接口请求，直接执行
- 数据库只读查询（SELECT），直接执行
- 写操作（INSERT/UPDATE/DELETE），先展示命令确认
- 读日志、读配置文件，直接执行
